<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Place one call into jambonz and play a wav file (pre-converted to an RTP pcap
  by tools/wav-to-rtp-pcap.py) as the caller's audio. No audio hardware and no
  softphone involved: the media is replayed packet-for-packet at the original
  timing, which is what makes this a faithful stand-in for a live call.

  Driven by tools/inject-call.sh.

  Note on auth: sipp's [authentication] keyword expands to the COMPLETE header,
  choosing Authorization for a 401 challenge and Proxy-Authorization for a 407.
  Do not prefix it with a header name — that yields a duplicated header name and
  the server rejects the credentials.

  Keywords supplied on the command line:
    -key realm <account sip realm>   -> [realm]
    -key pcap  <path to rtp pcap>    -> [pcap]
    -s <called number>               -> [service]
    -inf <csv of sip usernames>      -> [field0]
    -au / -ap                        -> digest credentials
    -d <ms>                          -> how long to hold the call after playback starts
-->
<scenario name="inject wav audio into jambonz">

  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[realm] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[realm]>;tag=[call_number]
      To: <sip:[service]@[realm]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      User-Agent: sipp-speechmatics-injector
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="183" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="407" auth="true" optional="true" next="challenged">
    <action>
      <!-- topmost Via in the response is the one we sent, so this recovers the
           branch of the INVITE being challenged -->
      <ereg regexp="branch=([^;,>[:space:]]*)" search_in="msg" assign_to="_all,invbranch"/>
    </action>
  </recv>
  <recv response="401" auth="true" optional="true" next="challenged">
    <action>
      <ereg regexp="branch=([^;,>[:space:]]*)" search_in="msg" assign_to="_all,invbranch"/>
    </action>
  </recv>
  <recv response="200" rtd="true" next="answered"/>

  <!--
    A challenge is a final response to the INVITE, so it must be ACKed. Per
    RFC 3261 17.1.1.3 that ACK belongs to the SAME transaction and must carry
    the INVITE's branch — sipp's [branch] would mint a fresh one, the server
    would not match the ACK, and it would retransmit the challenge until the
    scenario aborted on the unexpected duplicate.
  -->
  <label id="challenged"/>
  <send>
    <![CDATA[

      ACK sip:[service]@[realm] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[$invbranch]
      From: <sip:[field0]@[realm]>;tag=[call_number]
      To: <sip:[service]@[realm]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[realm] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[realm]>;tag=[call_number]
      To: <sip:[service]@[realm]>
      Call-ID: [call_id]
      CSeq: 2 INVITE
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=[transport]>
      [authentication]
      Max-Forwards: 70
      User-Agent: sipp-speechmatics-injector
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15
      a=sendrecv

    ]]>
  </send>
  <recv response="100" optional="true"/>
  <recv response="183" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="200" rtd="true"/>

  <label id="answered"/>
  <send>
    <![CDATA[

      ACK sip:[service]@[realm] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[realm]>;tag=[call_number]
      To: <sip:[service]@[realm]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] ACK
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- playback is non-blocking, so the pause below is what holds the call up -->
  <nop>
    <action>
      <exec play_pcap_audio="[pcap]"/>
    </action>
  </nop>

  <pause/>

  <send retrans="500">
    <![CDATA[

      BYE sip:[service]@[realm] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field0]@[realm]>;tag=[call_number]
      To: <sip:[service]@[realm]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] BYE
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true"/>

</scenario>
